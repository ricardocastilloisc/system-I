import { OrdenasPkPipe } from './ordenas-pk.pipe';

describe('OrdenasPkPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenasPkPipe();
    expect(pipe).toBeTruthy();
  });
});
