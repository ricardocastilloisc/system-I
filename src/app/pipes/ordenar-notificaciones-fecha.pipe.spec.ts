import { OrdenarNotificacionesFechaPipe } from './ordenar-notificaciones-fecha.pipe';

describe('OrdenarNotificacionesFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarNotificacionesFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
